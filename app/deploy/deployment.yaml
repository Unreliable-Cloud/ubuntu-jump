apiVersion: apps/v1
kind: Deployment
metadata:
  name: devpod
spec:
  selector:
    matchLabels:
      app: devpod
  template:
    metadata:
      labels:
        app: devpod
    spec:
      containers:
      - name: devpod
        image: docker.io/youvegotmoxie/devpod
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
        ports:
        - containerPort: 8000
          name: http
        readinessProbe:
          httpGet:
            port: http